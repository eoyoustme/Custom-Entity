---====== Load spawner ======---

local Spawner = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/DOORS-Entity-Spawner-V2/main/init.luau"))()

---====== Create entity ======---

local entity = Spawner:Create({
	Entity = {
		Name = "Defrost",
		Asset = "https://github.com/eoyoustme/Custom-Entity/blob/main/Defrost.rbxm?raw=true",
		HeightOffset = 2
	},
	Lights = {
		Flicker = {
			Enabled = false,
			Duration = 1
		},
		Shatter = true,
		Repair = false
	},
	Earthquake = {
		Enabled = true
	},
	CameraShake = {
		Enabled = true,
		Range = 100,
		Values = {1.5, 20, 0.1, 1} -- Magnitude, Roughness, FadeIn, FadeOut
	},
	Movement = {
		Speed = 180,
		Delay = 1,
		Reversed = false
	},
	Rebounding = {
		Enabled = true,
		Type = "Ambush", -- "Blitz"
		Min = 4,
		Max = 4,
		Delay = 0
	},
	Damage = {
		Enabled = true,
		Range = 40,
		Amount = 125
	},
	Crucifixion = {
		Enabled = true,
		Range = 40,
		Resist = false,
		Break = true
	},
	Death = {
		Type = "Guiding", -- "Curious"
		Hints = {"You die to who call Defrost", "If light turn to Blue go hide", "Or you will die", "in the frost"},
		Cause = "Defrost"
	}
})

---====== Debug entity ======---

entity:SetCallback("OnSpawned", function()
	print("Entity has spawned")
	local function GetGitSound(GithubSnd, SoundName)
		local url = GithubSnd
		if not isfile(SoundName .. ".mp3") then
			writefile(SoundName .. ".mp3", game:HttpGet(url))
		end
		local sound = Instance.new("Sound")
		sound.SoundId = (getcustomasset or getsynasset)(SoundName .. ".mp3")
		return sound
	end
	local Far = GetGitSound("https://github.com/eoyoustme/Custom-Entity/blob/main/Far.mp3?raw=true","Far")
	Far.Parent = game.Workspace.Defrost.RushNew
	Far.RollOffMinDistance = 200
	Far.RollOffMaxDistance = 400
	Far.Volume = 4
	Far.Looped = true
	Far.PlaybackSpeed = 1
	Far:Play()
	
	local Reboundcolor = Instance.new("ColorCorrectionEffect",game.Lighting) game.Debris:AddItem(Reboundcolor,24)
	Reboundcolor.Name = "Warn"
	Reboundcolor.TintColor = Color3.fromRGB(0, 0, 255) Reboundcolor.Saturation = -0.7 Reboundcolor.Contrast = 0.2
	game.TweenService:Create(Reboundcolor,TweenInfo.new(15),{TintColor = Color3.fromRGB(255, 255, 255),Saturation = 0, Contrast = 0}):Play()
	local TweenService = game:GetService("TweenService")
	local TW = TweenService:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5),{TintColor = Color3.fromRGB(255, 255, 255)})
	TW:Play()
end)

entity:SetCallback("OnStartMoving", function()
	print("Entity has started moving")
	local function GetGitSound(GithubSnd, SoundName)
		local url = GithubSnd
		if not isfile(SoundName .. ".mp3") then
			writefile(SoundName .. ".mp3", game:HttpGet(url))
		end
		local sound = Instance.new("Sound")
		sound.SoundId = (getcustomasset or getsynasset)(SoundName .. ".mp3")
		return sound
	end
	local Near = GetGitSound("https://github.com/eoyoustme/Custom-Entity/blob/main/Near.mp3?raw=true","Near")
	Near.Parent = game.Workspace.Defrost.RushNew
	Near.RollOffMinDistance = 100
	Near.RollOffMaxDistance = 200
	Near.Volume = 6
	Near.Looped = true
	Near.PlaybackSpeed = 1
	Near:Play()
end)

entity:SetCallback("OnEnterRoom", function(room: Model, firstTime: boolean)
	if firstTime == true then
		print("Entity has entered room: ".. room.Name.. " for the first time")
	else
		print("Entity has entered room: ".. room.Name.. " again")
	end
end)

entity:SetCallback("OnLookAt", function(lineOfSight: boolean)
	if lineOfSight == true then
		print("Player is looking at entity")
	else
		print("Player view is obstructed by something")
	end
end)

entity:SetCallback("OnRebounding", function(startOfRebound: boolean)
	if startOfRebound == true then
		print("Entity has started rebounding")
	else
		print("Entity has finished rebounding")
	end
end)

entity:SetCallback("OnDespawning", function()
	print("Entity is despawning")
	---====== Load achievement giver ======---
	local achievementGiver = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Custom%20Achievements/Source.lua"))()

	---====== Display achievement ======---
	achievementGiver({
		Title = "Dark blue deeps",
		Desc = "The depths never end",
		Reason = "Encounter Defrost",
		Image = "rbxassetid://106372365387916"
	})
end)

entity:SetCallback("OnDespawned", function()
	print("Entity has despawned")
end)

entity:SetCallback("OnDamagePlayer", function(newHealth: number)
	if newHealth <= 0 then
		print("Entity has killed the player")
		local RunService = game:GetService("RunService")
		local TweenService = game:GetService("TweenService")
		local Players = game:GetService("Players")
		local player = Players.LocalPlayer

		-- Tạo sound + effect giống bạn
		local sound = Instance.new("Sound", workspace)
		sound.SoundId = "rbxassetid://5263560566"
		sound.TimePosition = 0
		local level = Instance.new("DistortionSoundEffect", sound)
		level.Level = 0.97

		task.wait(0.2) -- ĐÃ FIX: Thay bằng task.wait để không bị trễ luồng xử lý LocalScript

		-- Tạo GUI
		local screen = Instance.new("ScreenGui", player:WaitForChild("PlayerGui"))

		local background = Instance.new("Frame", screen)
		background.AnchorPoint = Vector2.new(0.5, 0.5)
		background.Size = UDim2.new(10, 0, 10, 0)
		background.Position = UDim2.new(0.5, 0, 0.5, 0)
		background.BackgroundColor3 = Color3.fromRGB(0, 157, 255)

		local image = Instance.new("ImageLabel", screen)
		image.AnchorPoint = Vector2.new(0.5, 0.5)
		image.Position = UDim2.new(0.5, 0, 0.5, 0)
		image.BackgroundTransparency = 1
		image.Size = UDim2.new(0, 5, 0, 5)
		image.LayoutOrder = 0
		image.BorderSizePixel = 0
		image.Image = "http://www.roblox.com/asset/?id=110059689403349"
		image.Rotation = 0

		-- flash màu background như bạn
		task.spawn(function()
			while background and background.Parent do
				background.BackgroundColor3 = Color3.fromRGB(0, 0, 105)
				task.wait(0.08)
				if not background or not background.Parent then break end
				background.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
				task.wait(0.08)
			end
		end)

		-- play sound
		sound:Play()

		-- Tween scale ảnh (giữ nguyên thời gian và kích thước khổng lồ của bạn)
		local tween = TweenService:Create(image, TweenInfo.new(65, Enum.EasingStyle.Linear), {Size = UDim2.new(0, 130000, 0, 130000)})
		tween:Play()

		-- ====== Phần thêm: oscillation (rotation + position jitter) ======
		-- ĐÃ FIX: Loại bỏ toàn bộ khoảng trắng lỗi Unicode ẩn ở khu vực này
		local ROT_AMPLITUDE = 12
		local PERIOD = 0.1
		local JITTER_X = 0
		local JITTER_Y = 0

		-- bắt đầu time
		local t0 = tick()
		local conn
		conn = RunService.RenderStepped:Connect(function()
			-- nếu image đã bị xóa thì ngắt connection
			if not image or not image.Parent then
				if conn then conn:Disconnect() end
				return
			end

			local elapsed = tick() - t0
			-- sinus để quay mượt: sin(2*pi * elapsed / PERIOD) in [-1,1]
			local s = math.sin(0.5 * math.pi * elapsed / PERIOD)
			image.Rotation = ROT_AMPLITUDE * s

			-- position jitter xung quanh center (UDim2.new(0.5, offsetX, 0.5, offsetY))
			local offsetX = math.sin(2 * math.pi * elapsed / (PERIOD * 1.3)) * JITTER_X -- tỉ lệ khác 1.3 để không hoàn toàn đồng bộ với rotation
			local offsetY = math.cos(2 * math.pi * elapsed / (PERIOD * 1.1)) * JITTER_Y
			image.Position = UDim2.new(0.5, offsetX, 0.5, offsetY)
		end)
		-- =================================================================

		task.wait(0.8)
		if sound then sound:Destroy() end
		if screen then screen:Destroy() end
		if conn then
			pcall(function() conn:Disconnect() end)
		end
	else
		print("Entity has damaged the player")
	end
end)

--[[

DEVELOPER NOTE:
By overwriting 'CrucifixionOverwrite' the default crucifixion callback will be replaced with your custom callback.

entity:SetCallback("CrucifixionOverwrite", function()
    print("Custom crucifixion callback")
end)

]]--

---====== Run entity ======---

entity:Run(true)
